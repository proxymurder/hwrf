map $http_x_forwarded_proto $proxy_x_forwarded_proto {
    default $http_x_forwarded_proto;
    '' $scheme;
}
map $http_x_forwarded_port $proxy_x_forwarded_port {
    default $http_x_forwarded_port;
    '' $server_port;
}
map $http_upgrade $proxy_connection {
    default upgrade;
    '' close;
}
map $scheme $proxy_x_forwarded_ssl {
    default off;
    https off;
}

server {
    listen 443 ssl http2;
    server_name default.local;

    access_log /var/log/nginx/default.local.access.log;
    error_log /var/log/nginx/default.local.error.log error;

    include /etc/nginx/extra/https.conf;

    location / {
        proxy_pass https://php-www;
        include /etc/nginx/extra/proxy.conf;
    }
}

server {
    listen 443 ssl http2;
    server_name wss.default.local;

    access_log /var/log/nginx/wss.default.local.access.log;
    error_log /var/log/nginx/wss.default.local.error.log error;

    include /etc/nginx/extra/https.conf;

    location / {
        proxy_pass https://socket:5555;
        include /etc/nginx/extra/proxy.conf;
    }
}

server {
    listen 443 ssl http2;
    server_name app.default.local;

    access_log /var/log/nginx/app.default.local.access.log;
    error_log /var/log/nginx/app.default.local.error.log error;

    include /etc/nginx/extra/https.conf;

    location / {
        proxy_pass https://app-www;
        include /etc/nginx/extra/proxy.conf;
    }
}

server {
    listen 80;
    server_name default.local;
    return 301 https://$host$request_uri;
}

server {
    listen 80;
    server_name wss.default.local;
    return 301 https://$host$request_uri;
}

server {
    listen 80;
    server_name app.default.local;
    return 301 https://$host$request_uri;
}