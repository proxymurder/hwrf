server {
    listen 443 ssl http2;
    server_name ws.default.local;

    access_log /var/log/nginx/ws.default.local.access.log;
    error_log /var/log/nginx/ws.default.local.error.log error;

    include /etc/nginx/https.conf;

    location / {
        proxy_pass https://sockjs:5000;
        include /etc/nginx/proxy.conf;
    }
}

server {
    listen 80;
    server_name ws.default.local;
    return 301 https://$host$request_uri;
}