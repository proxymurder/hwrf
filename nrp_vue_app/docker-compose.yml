version: "3.9"
networks: 
 local:
  driver: bridge
services:
 www:
  image: nginx
  volumes:
   - ./app/dist:/usr/share/nginx/html
   - ./etc/nginx/conf.d/default.conf:/etc/nginx/conf.d/default.conf
  ports:
   - 80
  networks:
   - local