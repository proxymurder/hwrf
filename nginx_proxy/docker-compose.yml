version: "3.9"
networks:
 api:
  external:
   name: api_local
 app:
  external:
   name: app_local
services:
 www:
  image: nginx
  volumes:
    - ./nginx/nrp-dev.conf:/etc/nginx/conf.d/nrp-dev.conf
    - ./nginx/opt:/etc/nginx/opt
    - ./ssl/certs:/etc/ssl/certs/nginx
  networks:
    - api
    - app
  ports:
    - 80:80
    - 443:443
